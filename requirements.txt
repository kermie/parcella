fastapi==0.130.0
# Pinned explicitly, not left to FastAPI's range: Starlette 1.x removed
# the legacy TemplateResponse(name, context) signature that every router
# here still uses. 0.49.x is the newest line that keeps it and carries
# the multipart-DoS fixes that mattered (PYSEC-2026-1941/1942/1943);
# moving to Starlette 1.x is a separate migration, see ADR 0064.
starlette==0.49.3
uvicorn[standard]==0.30.6
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
alembic==1.13.2
jinja2==3.1.6
python-multipart==0.0.32
itsdangerous==2.2.0
bcrypt==4.2.0
pyjwt==2.13.0
cryptography==50.0.0
httpx==0.27.2
aiosmtplib==5.1.1
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.2.2
email-validator==2.2.0
babel==2.16.0
icalendar==6.1.0
bleach==6.4.0
markdown==3.10.3
weasyprint==69.0
qrcode[pil]==8.2
